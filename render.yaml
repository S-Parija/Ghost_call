services:
  - type: web
    name: ghostcall
    env: python
    plan: free
    buildCommand: "pip install uv && uv sync --frozen"
    startCommand: "uv run fastapi run main.py --host 0.0.0.0 --port $PORT"
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
